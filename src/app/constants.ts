export enum Method {
  get = 'GET',
  post = 'POST',
  put = 'PUT',
  delete = 'DELETE',
}

export enum StatusCode {
  success = 200,
  created = 201,
  deleted = 204,
  invalid = 400,
  notFound = 404,
}

export const PORT_RESERVE = '4000';
